{
  "name": "data-explorer",
  "version": "1.0.0",
  "description": "CSV data explorer — search, filter, sort, visualize",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "csv-parse": "^5.5.3"
  }
}
